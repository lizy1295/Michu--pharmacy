import Placeholder from '../_placeholder';

export default function AdminInventoryPage() {
  return <Placeholder title="Inventory" description="Monitor stock levels, low stock alerts, expired medicines, and incoming inventory." />;
}
